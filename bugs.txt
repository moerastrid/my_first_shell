Bug 1:
	Heredoc ctrl-c bug

Commands to reproduce bug:
	cat << EOF
		ctrl-c
	cat << EOF
		enter

Expected behavior:
	Give prompt to fill heredoc.
Current behavior:
	Heredoc prompt is closed.



Bug 2:
	Heredoc ctrl-c "bug"

Commands to reproduce bug:
	cat << EOF
		ctrl-C
	ctrl-D

Expected behavior:
	Minishell is closed

Current behavior:
	Minishell heredoc subprompt is closed. (press ctrl-D again to actually close minishell)



Bug 3:
	pipe bug

Commands to reproduce bug:
	ls |

Expected behavior:
	Probably syntax error

Current behavior:
	ls output / no output , no error message



Bug 5:
	empty quote

Commands to reproduce bug:
	""

	"" cmd

Expected behavior:
empty command + error message ? (can't test, different version)
error code 127 ?

Current behavior:
Probably wrong error message, error code
Need to check codam bash version to fix.


Bug 6:

Commands to reproduce bug:

Expected behavior:

Current behavior:



Bug 7:

Commands to reproduce bug:

Expected behavior:

Current behavior:
